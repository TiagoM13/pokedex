/* eslint-disable consistent-return */
import { THEME } from '../theme/theme';

export const getTypesToColor = (type: string) => {
  if (type === THEME.TYPES.GRASS) {
    return THEME.COLORS.GRASS;
  }

  if (type === THEME.TYPES.FIRE) {
    return THEME.COLORS.FIRE;
  }

  if (type === THEME.TYPES.WATER) {
    return THEME.COLORS.WATER;
  }

  if (type === THEME.TYPES.BUG) {
    return THEME.COLORS.BUG;
  }

  if (type === THEME.TYPES.FLYING) {
    return THEME.COLORS.FLYING;
  }

  if (type === THEME.TYPES.DARK) {
    return THEME.COLORS.DARK;
  }

  if (type === THEME.TYPES.DRAGON) {
    return THEME.COLORS.DRAGON;
  }

  if (type === THEME.TYPES.ELECTRIC) {
    return THEME.COLORS.ELECTRIC;
  }

  if (type === THEME.TYPES.FAIRY) {
    return THEME.COLORS.FAIRY;
  }

  if (type === THEME.TYPES.FIGHTING) {
    return THEME.COLORS.FIGHTING;
  }

  if (type === THEME.TYPES.GHOST) {
    return THEME.COLORS.GHOST;
  }

  if (type === THEME.TYPES.GROUND) {
    return THEME.COLORS.GROUND;
  }

  if (type === THEME.TYPES.ICE) {
    return THEME.COLORS.ICE;
  }

  if (type === THEME.TYPES.NORMAL) {
    return THEME.COLORS.NORMAL;
  }

  if (type === THEME.TYPES.POISON) {
    return THEME.COLORS.POISON;
  }

  if (type === THEME.TYPES.PSYCHIC) {
    return THEME.COLORS.PSYCHIC;
  }

  if (type === THEME.TYPES.ROCK) {
    return THEME.COLORS.ROCK;
  }

  if (type === THEME.TYPES.STEEL) {
    return THEME.COLORS.STEEL;
  }
};

export const getTypesToBackgroundColor = (type: string) => {
  if (type === THEME.TYPES.GRASS) {
    return THEME.BACKGROUND.GRASS;
  }

  if (type === THEME.TYPES.FIRE) {
    return THEME.BACKGROUND.FIRE;
  }

  if (type === THEME.TYPES.WATER) {
    return THEME.BACKGROUND.WATER;
  }

  if (type === THEME.TYPES.BUG) {
    return THEME.BACKGROUND.BUG;
  }

  if (type === THEME.TYPES.FLYING) {
    return THEME.BACKGROUND.FLYING;
  }

  if (type === THEME.TYPES.DARK) {
    return THEME.BACKGROUND.DARK;
  }

  if (type === THEME.TYPES.DRAGON) {
    return THEME.BACKGROUND.DRAGON;
  }

  if (type === THEME.TYPES.ELECTRIC) {
    return THEME.BACKGROUND.ELECTRIC;
  }

  if (type === THEME.TYPES.FAIRY) {
    return THEME.BACKGROUND.FAIRY;
  }

  if (type === THEME.TYPES.FIGHTING) {
    return THEME.BACKGROUND.FIGHTING;
  }

  if (type === THEME.TYPES.GHOST) {
    return THEME.BACKGROUND.GHOST;
  }

  if (type === THEME.TYPES.GROUND) {
    return THEME.BACKGROUND.GROUND;
  }

  if (type === THEME.TYPES.ICE) {
    return THEME.BACKGROUND.ICE;
  }

  if (type === THEME.TYPES.NORMAL) {
    return THEME.BACKGROUND.NORMAL;
  }

  if (type === THEME.TYPES.POISON) {
    return THEME.BACKGROUND.POISON;
  }

  if (type === THEME.TYPES.PSYCHIC) {
    return THEME.BACKGROUND.PSYCHIC;
  }

  if (type === THEME.TYPES.ROCK) {
    return THEME.BACKGROUND.ROCK;
  }

  if (type === THEME.TYPES.STEEL) {
    return THEME.BACKGROUND.STEEL;
  }
};
